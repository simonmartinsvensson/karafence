/**
 * Active tower-skin tint (set by GameScene from meta.activeSkin at run start),
 * read by Tower when it builds its body sprite. 0xffffff = no recolour.
 */
export const skin = { tint: 0xffffff };
